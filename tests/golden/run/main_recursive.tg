// Test run pipeline with recursive function
// Verifies evaluation handles recursion correctly

pub fn factorial(n: Nat) -> Nat {
    if n == 0 {
        1
    } else {
        n * factorial(n - 1)
    }
}

pub fn main() -> Nat {
    factorial(5)
}
